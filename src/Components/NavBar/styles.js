import { makeStyles } from '@mui/material';

export default makeStyles(() => ({
  toolbar: {
    display: 'flex',
    height: '80px',
    justifyContent: 'space-between',
    marginLeft: '240px',
  },
}));
